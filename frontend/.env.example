# No environment variables are required. /api/* is proxied to the shared
# bookaride.co.nz platform (see vercel.json). Set VITE_BACKEND_URL only to
# point the booking page at a different API origin during local development.
# VITE_BACKEND_URL=http://localhost:3001
